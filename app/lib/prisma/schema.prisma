generator client {
  provider = "prisma-client"
  output   = "./generated"
}

datasource db {
  provider = "postgresql"
}

model user {
  id    Int    @id @default(autoincrement())
  nome  String
  email String @unique
  senha String
  foto  String
}
